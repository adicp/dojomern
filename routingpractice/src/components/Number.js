import React from 'react';

const Number = (props) => {
    // console.log(props);
    return (
        <div>
            <h1>The number is: {props.number}</h1>
        </div>
    );
};

export default Number;